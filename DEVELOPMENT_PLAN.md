# Soda Prompt Hub 开发计划

## 1. 产品定位

Soda Prompt Hub 是运行在 Mac 上的本地 AI 绘图创作与知识中枢。它不替代 Windows 上的 ComfyUI，也不直接取代 OC Manager，而是把三类资料组织成可使用的创作结果：

- OC Manager 中的角色、世界观和角色专属 Prompt；
- 本地提示词库、标签库、画风库和视觉参考图；
- 用户输入的中文创作想法及本地模型生成的补充内容。

当前阶段首先完成绘图工作流。视频创作预留模型 Profile 和项目类型扩展点，但 MiniMax H3 界面、提示词编排和生成连接均延后。

## 2. 最终使用流程

绘图主流程固定为：

1. 选择创作起点：空白想法、已有 OC、已有配方或视觉参考；
2. 输入中文创作意图；
3. 从本地库检索并选择角色、服装、动作、构图、场景、灯光和画风参考；
4. 在七个语义槽位中编辑、锁定和组合内容；
5. 选择目标模型 Profile；
6. 生成与目标模型匹配的 Prompt；
7. 复制、保存配方或导出生成包；
8. 后续发送到 Windows 绘梦台或 ComfyUI 执行。

核心原则是“保存一份创作意图，生成多种模型版本”，而不是把某个模型的 Prompt 当作项目本体。

绘图 V1.4 在版本链上增加父版本对照和显式建议应用：只展示有变化或有复盘建议的槽位，建议只填入空且未锁定内容，不自动覆盖用户判断。

## 3. 信息架构

### 3.1 开始创作

默认首页，面向第一次使用和日常创作。页面按步骤呈现，不先展示数据库管理信息。

主要区域：

- 创作起点；
- 中文想法；
- OC 选择；
- 七槽位编辑器；
- 视觉参考选择区；
- 目标模型切换；
- Prompt 预览与导出；
- 当前项目保存状态。

### 3.2 提示词与视觉库

保留现有 Prompt Archive 的检索、来源、成人分级、收藏、评分和备注能力，增加“用于创作”动作：

- 加入角色；
- 加入服装；
- 加入动作；
- 加入构图；
- 加入场景；
- 加入灯光；
- 加入画风；
- 作为参考图。

类型、来源和安全等级属于高级筛选，默认折叠。

### 3.3 角色库

保留 OC Manager JSON 只读交换机制，增加：

- 一键以该角色开始创作；
- 从角色外观、服装、身份和角色 Prompt 填充槽位；
- 展示来源世界和更新时间；
- 保存本次创作与角色的关联，但不回写 OC Manager。

### 3.4 配方与历史

保存可复现的创作状态：

- 原始中文想法；
- 七槽位内容及锁定状态；
- 目标模型 Profile；
- 正面和负面 Prompt；
- 视觉参考；
- OC 关联；
- 个人备注、评分和测试结果；
- 后续可选的 ComfyUI 参数与工作流标识。

### 3.5 资料管理

集中放置不应出现在创作首页的维护功能：

- 资料来源与条目数量；
- 重建本地索引；
- Git 资料源更新；
- OC JSON 导入；
- 缩略图状态；
- 数据库与个人资料备份；
- LM Studio、Windows 和 ComfyUI 连接状态。

## 4. 核心数据结构

### 4.1 创作项目 CreativeProject

创作项目保存语义，不绑定单一模型：

- `project_type`: 当前为 `image`，未来可增加 `video`；
- `title`；
- `brief_zh`；
- `character_id`；
- `safety_mode`: `sfw`、`suggestive`、`adult`、`explicit-adult`；
- `slots`: character、outfit、action、composition、scene、lighting、style；
- `slot_locks`；
- `references`；
- `target_profile`；
- `created_at`、`updated_at`。

### 4.2 模型 Profile

模型 Profile 负责把统一语义转换成目标模型的成品 Prompt，并保存以下规则：

- 输出形态：标签、自然语言或编辑指令；
- 槽位顺序与组合方式；
- 质量词与触发词策略；
- 权重语法；
- negative prompt 策略；
- 成人场景处理；
- 可选模型版本和 LoRA 提示；
- 输出验证规则。

首批 Profile：

1. `anima`：Danbooru/Booru 标签、权重、正负面词；
2. `krea2`：连贯自然语言，兼容 Clio 画风和 Krea 2 Prompt Enhancement；
3. `semantic-general`：为 FLUX、Qwen-Image、Z-Image 等自然语言模型预留，共享基础结构但允许模型级覆盖；
4. `classic-clip`：兼容现有 SD1.5、SDXL 和旧资料。

视频阶段再增加 `minimax-h3`，不在绘图阶段实现。

### 4.3 资料条目

一条资料不能只保存单一字符串，应允许同时存在：

- 原始文本；
- 标准化标签；
- 自然语言 caption；
- 语义槽位分类；
- 模型适配信息；
- 来源、版权与原链接；
- 成人分级；
- 本地缩略图和原图；
- 用户收藏、评分、备注和实测记录。

## 5. 绘图阶段路线

### 阶段 0：规划与基线冻结

目标：明确范围，保护现有能力。

交付：

- 本开发计划；
- 现有 API、数据表和页面行为清单；
- 回归测试基线；
- 确认不自动改动 OC Manager、公用 Git 资料源和 Windows 文件。

验收：当前检索、视觉缩略图、收藏、评分、备注、OC 导入和 MCP 工具均有可验证基线。

### 阶段 1：导航与第一次使用

目标：用户打开页面后立即知道从哪里开始。

实现：

- 增加“开始创作”默认首页；
- 主导航调整为开始创作、资料库、角色库、配方、资料管理；
- 增加可关闭、可重新打开的新手引导；
- 提供一个可一键载入的示例项目；
- 把统计、来源、重建索引移入资料管理；
- 将主要操作改为中文行动词，英文只作视觉辅助。

验收：第一次打开时，不阅读 README 也能完成“输入想法—看到下一步—进入资料选择”。

### 阶段 2：创作项目与七槽位编辑器

目标：建立与模型无关的创作草稿。

实现：

- 新建、自动保存、重命名和恢复创作项目；
- 七槽位编辑器及逐项锁定；
- 原始中文想法与槽位内容分离；
- 项目级成人分级；
- 未保存状态、清空和恢复提示；
- 数据库迁移与项目 API。

验收：刷新页面后项目可恢复；锁定槽位不会被批量导入或模型辅助覆盖。

### 阶段 3：Anima 与 Krea 2 双 Profile

目标：同一项目生成两种真正不同的模型 Prompt。

实现：

- Anima 标签编译器；
- Krea 2 自然语言编译器；
- 模型切换和双版本预览；
- 正面、负面 Prompt 分离；
- 成人模式下不自动加入与目标冲突的 `nsfw`、`nude` 负面词；
- Clio 画风、LoRA trigger word 与 Prompt Enhancement 提示；
- 格式验证、复制和导出。

验收：同一中文需求生成结构正确的 Anima 标签版与 Krea 2 自然语言版，二者不是机械互译。

### 阶段 4：资料库进入创作闭环

目标：资料不只是可搜索，而是能被直接使用。

实现：

- 搜索结果增加七种“加入槽位”动作；
- 建立创作资料篮；
- 视觉卡片可作为角色、服装、构图、场景或画风参考；
- 标签与 caption 双表示；
- 收藏和实测备注在创作时可见；
- 保留高级筛选和完整档案浏览模式。

验收：用户能从搜索结果选图/选词，填入槽位并立即看到两个模型版本的变化。

### 阶段 5：OC Manager 创作连接

目标：让角色资料直接成为绘图起点。

实现：

- 角色卡增加“以此角色开始创作”；
- 角色外观、服装和已有 Prompt 映射到槽位；
- 世界与角色关系作为上下文，不全部塞入成品 Prompt；
- 支持选择角色图库作为视觉参考；
- 保持单向导入，默认不回写 OC Manager。

验收：从一个 OC 角色出发，可以生成 Anima 和 Krea 2 两种可用 Prompt，同时保留角色身份关联。

### 阶段 6：配方、实测与数据集辅助

目标：形成可复现、可积累的个人工作流。

实现：

- 保存、复制、收藏和版本化配方；
- 记录使用模型、Seed、尺寸、工作流和结果图；
- 对 Prompt 和视觉参考增加实测结论；
- 支持从满意结果反向生成新配方；
- 支持从本地结果图手动精选训练素材；
- 按 Anima tags / Krea 2 自然语言生成同名 caption；
- 允许按图片、按 Profile 覆盖 caption；
- 导出原图、同名 `.txt` 与可追踪 `manifest.json` 的本地 ZIP。

验收：能够回答“这张图用了哪个角色、哪些参考、哪个 Prompt 版本和哪些参数”，并能把手动精选结果打包成可转交 Windows 的本地数据集。

### 阶段 7：本地模型辅助

目标：让 LM Studio 帮助整理，而不是取代用户决策。

实现：

- 从中文想法拆分七槽位；
- 根据目标模型生成 Profile 输出；
- 看图反推标签、自然语言 caption 和问题诊断；
- 锁定内容保护；
- 显示模型修改前后的差异；
- 默认由用户确认后写入项目。

验收：本地模型可以辅助填充，但不会静默覆盖锁定内容或原始资料。

### 阶段 8：Windows 与 ComfyUI 交付

目标：把 Mac 上完成的生成包交给 Windows 执行。

实现顺序：

1. 导出标准 JSON；
2. 手动导入 Windows 绘梦台；
3. 局域网发送；
4. ComfyUI 队列与结果回传；
5. 远程状态、失败重试和连接诊断。

验收：网络断开不影响 Mac 创作和导出；网络连接后可以明确看到目标设备、工作流和提交结果。

## 6. 视频阶段（延后）

绘图阶段只做以下预留：

- `project_type` 支持未来增加 `video`；
- Profile 注册机制不写死图片模型；
- 参考资料允许图片、视频和音频类型；
- 导出格式允许版本扩展。

绘图主线验收前，不开发：

- MiniMax H3 视频编辑器；
- 动作时间线、镜头、声音和结尾槽位；
- 文生视频、图生视频、首尾帧和全参考模式；
- H3 API、网页自动化或 ComfyUI H3 节点连接。

## 7. 非功能要求

- 本地优先：核心检索、项目编辑和导出断网可用；
- 隐私：OC、成人项目和个人备注不上传，除非用户明确选择外部服务；
- 可恢复：数据库迁移前备份，项目自动保存；
- 不破坏来源：公共 Git 资料和 OC Manager 原始文件只读；
- 渐进披露：新用户只看到必要步骤，高级筛选和维护功能可展开；
- 可测试：Profile 编译器、数据库迁移、OC 映射和导出格式必须有自动测试；
- 可扩展：新增模型通过 Profile 注册，不复制整套页面。

## 8. 当前优先级

近期只推进以下闭环：

> 开始创作 → 七槽位 → Anima/Krea 2 → 资料引用 → OC 引用 → 结果复盘 → 版本迭代 → 精选数据集

在这个闭环稳定前，暂不优先处理：

- MiniMax H3 视频；
- 直接远程控制 Windows；
- 大规模模型列表；
- 复杂工作流市场；
- 多用户、云同步和公开分享。

## 9. 阶段完成标准

绘图 V1 可视为完成，需要同时满足：

1. 新用户能在界面引导下完成一次创作；
2. 同一项目可正确输出 Anima 与 Krea 2 Prompt；
3. 可以从视觉库和提示词库向槽位添加资料；
4. 可以从 OC Manager 角色开始创作；
5. SFW 与合法成人向项目不会使用互相冲突的固定负面词；
6. 项目、配方、引用来源和个人备注可保存与恢复；
7. 现有检索、收藏、评分、备注、OC 导入和 MCP 功能无回归；
8. 可以导出一个不依赖 Windows 在线状态的生成包。

## 10. V1.6 之后的开发主线

V1.6 已完成“结果图精选 → WD14 草稿 → 人工审核 → Anima caption → 数据集 ZIP”。后续不再继续给单图结果卡堆功能，而是把它扩展成完整的数据集与训练项目闭环。

### 10.1 工程安全门

当前业务文件已明显集中，且工作树尚未建立受 Git 跟踪的正式基线。进入下一轮较大开发前先完成：

- 正式库、用户媒体和配置备份；
- 在用户确认后建立 Git 基线与远程备份；
- 按领域拆分 API 和前端模块；
- 建立批量任务的进度、取消、恢复和失败记录。

### 10.2 V1.7 数据集工作台

第一优先级。允许导入一个现有图片目录，检查图片与 caption 配对、损坏文件、孤立文件、尺寸分布、标签频率和重复图片；WD14 改为可恢复批量队列，并保留人工审核与版本快照。所有操作默认只读原目录，输出写入新版本目录。

### 10.3 V1.8 LoRA 项目管理

在数据集工作台之上增加角色、服装、角色服装组合与风格项目；从 OC Manager 接收角色来源，定义 trigger word 和特征边界，使用覆盖矩阵检查训练集缺口，分别维护 Anima 与 Krea 2 caption，最终冻结带哈希与 manifest 的训练包。

### 10.4 V1.9 ComfyUI 结果回流

先读取用户手动放入本地/共享目录的 ComfyUI 图片与内嵌元数据，把 workflow、prompt、seed、sampler、模型和 LoRA 关联回项目与配方。此阶段不要求 SSH，也不依赖 Windows 常驻在线。

### 10.5 V2.0 Windows 执行桥

使用共享目录把生成包和数据集交付包统一交给 5060 Ti 16GB 节点，并接收结果与状态；自动 Worker 当前覆盖 ComfyUI 生成和 LoRA 只读清单，训练继续由 Windows AnimaLoraStudio 执行。Mac 始终保留离线编辑、审核、版本和导出能力。

### 10.6 V2.1 智能资料扩展

在可解释的 SHA-256/pHash 去重稳定后，再引入 CLIP embedding 相似图检索、视觉聚类、带来源的网络/Git 资料缓存，以及 Krea 2 异步 VLM caption。慢速任务可按需交给 Windows，但审核与版本管理仍在 Mac。

## 11. 每个版本的固定实施顺序

1. 冻结本轮范围与不做事项；
2. 备份正式数据并建立隔离测试资料；
3. 先实现数据模型和领域逻辑，再接 API 与页面；
4. 用自动测试验证路径、版本隔离和不覆盖原文件；
5. 在隔离服务完成桌面、手机和真实图片 QA；
6. 再更新 8765 正式服务，由用户用真实项目试用；
7. 记录发现、风险和下一阶段，不自动 commit、push 或创建 PR。
