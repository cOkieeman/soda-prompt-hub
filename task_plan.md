# 任务计划：Soda Prompt Hub 绘图创作中枢

## 目标

在保留现有资料检索、视觉参照、个人收藏和 OC Manager 导入能力的基础上，完成以绘图为优先的引导式创作台，使同一创作项目可输出适配 Anima 与 Krea 2 的 Prompt；视频功能延后。

## 当前阶段

阶段 15：V1.6 数据集自动打标与人工审核（已完成）

当前执行阶段：阶段 16 工程安全门；完成后进入阶段 17 / V1.7 数据集工作台

## 各阶段

### 阶段 0：规划与基线冻结
- [x] 明确产品定位与设备分工
- [x] 明确绘图优先、视频延后
- [x] 写入整体开发计划
- [x] 审计现有 API、数据库、页面和测试基线
- [x] 形成回归检查清单
- **状态：** complete

### 阶段 1：导航与第一次使用
- [x] 确定渐进拆分方案，静态资源抽取作为阶段 2 前置工作
- [x] 增加“开始创作”默认首页
- [x] 重组主导航与资料管理入口
- [x] 增加新手引导和真实示例任务
- [x] 验证移动端和桌面端入口清晰度
- **状态：** complete

### 阶段 2：创作项目与七槽位编辑器
- [x] 设计并迁移 CreativeProject 数据结构
- [x] 实现项目保存、恢复和锁定状态
- [x] 实现角色、服装、动作、构图、场景、灯光、画风槽位
- [x] 实现项目级成人分级
- **状态：** complete

### 阶段 3：Anima 与 Krea 2 双 Profile
- [x] 实现 Profile 注册与编译接口
- [x] 实现 Anima 标签输出
- [x] 实现 Krea 2 自然语言输出
- [x] 实现正负面词、成人模式和格式校验
- **状态：** complete

### 阶段 4：资料库进入创作闭环
- [x] 从提示词卡片加入指定槽位
- [x] 从视觉卡片加入指定参考类别
- [x] 实现创作资料篮
- [x] 保留完整档案浏览与高级筛选
- **状态：** complete

### 阶段 5：OC Manager 创作连接
- [x] 从角色卡开始创作
- [x] 将角色资料映射到槽位
- [x] 将角色图库作为视觉参考
- [x] 保持只读交换和来源追踪
- **状态：** complete（当前真实库无 OC，使用导入/API 样例验证）

### 阶段 6：配方、实测与数据集辅助
- [x] 保存与版本化配方
- [x] 记录模型参数、结果图路径/链接和实测备注
- [x] 支持满意结果复用
- [x] 在生成包中保留 slots/caption/tag 扩展基础
- **状态：** complete（结果图自动回传延后到 Windows 直连）

### 阶段 7：本地模型辅助
- [x] 接入 LM Studio 辅助拆分和转换
- [x] 实现锁定保护与建议预览
- [x] 允许视觉反推文本进入槽位与参考篮
- [x] 默认由用户确认后写入项目
- **状态：** complete

### 阶段 8：Windows 与 ComfyUI 交付
- [x] 导出标准生成包 JSON
- [x] 写明并验证字段级手动导入顺序
- [ ] 局域网发送和结果回传（待两台 Windows 的固定地址与传输方式）
- **状态：** partial / external details needed

### 阶段 9：V1.1 本地智能取材
- [x] 将中文创作想法拆成七槽位本地检索词
- [x] 以项目成人分级过滤并排序真实资料候选
- [x] 按槽位展示文字、视觉参照、来源与匹配依据
- [x] 只在用户确认后加入槽位和参考篮
- [x] 可选调用已加载模型扩展英文检索词
- [x] 完成桌面、手机与真实资料库验收
- **状态：** complete

### 阶段 10：V1.2 结果图复盘与配方迭代
- [x] 审计现有媒体存储与 LM Studio 视觉模型能力
- [x] 本地导入结果图并保留项目关联、分级与来源
- [x] 由本地视觉模型输出七槽位观察、问题诊断与改进建议
- [x] 建议先预览，只在用户确认后写回槽位或实测备注
- [x] 支持从满意结果生成下一版项目/配方的可追踪基础
- [x] 完成桌面、手机、隐私边界与真实服务验收
- **状态：** complete

### 阶段 11：V1.3 创作版本链与下一轮分支
- [x] 审计项目 JSON、结果图元数据与配方快照的可复用边界
- [x] 实现从复盘结果创建下一版项目并记录父项目、源结果图与迭代号
- [x] 新版本保留槽位、锁定、参考与参数，但不复制上一轮结果图
- [x] 在项目列表和复盘面板显示版本来源与下一步入口
- [x] 完成 API、回归测试、桌面/手机验收并部署到 8765
- **状态：** complete

### 阶段 12：V1.4 版本对照与建议应用
- [x] 在 lineage 中保留七槽位视觉观察，并兼容既有空 lineage/旧分支
- [x] 复用现有单项目读取 API，并实现稳定的版本差异构造
- [x] 在派生项目显示上一版/当前版槽位变化与复盘建议
- [x] 只在用户确认后，将建议填入空且未锁定槽位
- [x] 完成 API、回归测试、桌面/手机验收并部署到 8765
- **状态：** complete

### 阶段 13：V1.5 结果精选与数据集导出
- [x] 审计现有结果图片、版本输出与导出目录结构
- [x] 为结果图片增加“加入数据集”和单图 caption 覆盖信息
- [x] 支持 Anima tags / Krea 2 自然语言两种 caption Profile
- [x] 导出精选原图、同名 `.txt` 与 `manifest.json` 的本地 ZIP
- [x] 在结果画廊加入精选、caption 编辑与导出入口
- [x] 补齐 API、ZIP 内容、路径安全和页面回归测试
- [x] 完成桌面/移动端浏览器 QA，并更新正式服务
- **状态：** complete

### 阶段 14：V1.6 前置安装——WD14 本地打标器
- [x] 建立独立模型目录并下载官方 SwinV2 V3 ONNX 与标签表
- [x] 为 Prompt Hub 增加可复现的 ONNX Runtime 依赖
- [x] 校验模型文件、输入输出和标签分类
- [x] 使用本机现有图片完成真实打标与置信度测试
- [x] 记录模型位置、默认阈值和后续接入边界
- **状态：** complete

### 阶段 15：V1.6 数据集自动打标与人工审核
- [x] 在结果精选区增加“WD14 自动打标”入口
- [x] 支持单图和精选图片批量打标
- [x] 将模型建议与人工 caption 分开保存，确认后才覆盖导出 caption
- [x] 提供阈值、标签删改、rating/character 保留策略
- [x] 完成真实数据集、桌面/手机与导出回归验收
- **状态：** complete

### 阶段 16：工程安全门与可维护性整理
- [x] 为当前未跟踪工作树建立可恢复的 Git 基线（仅本地 commit；未配置 remote、未 push）
- [x] 备份正式 SQLite、个人媒体、模型配置与资料源版本信息
- [ ] 将 `api.py`、`creative_web.py` 按数据集/创作/系统状态拆分边界
- [ ] 为长任务建立后台任务、进度、取消与失败重试基础
- [ ] 保持 8765 正式服务、现有 API 和页面行为兼容
- **状态：** in_progress

### 阶段 17：V1.7 数据集工作台
- [ ] 以只读方式导入任意本地图片数据集目录，不移动或覆盖原文件
- [ ] 检查图片解码、尺寸、同名 caption、孤立图片与孤立 `.txt`
- [ ] 增加缩略图总览、筛选、多选和数据集级审核状态
- [ ] 将 WD14 扩展为可恢复批量队列，不再受当前 24 张同步上限约束
- [ ] 增加标签频率、批量增删、冲突标签提示和 caption 版本快照
- [ ] 首版以 SHA-256 + pHash 完成完全重复与近似重复检测
- [ ] 导出前生成数据集清单、审计报告和不覆盖原始文件的版本包
- **状态：** pending / next product milestone

### 阶段 18：V1.8 角色 / 服装 / 风格 LoRA 项目管理
- [ ] 支持 character、outfit、character_outfit、style 四类项目
- [ ] 定义 trigger word、固定特征、可控变化、允许变化与禁止漂移
- [ ] 从 OC Manager 角色创建 LoRA 项目，但不回写 OC Manager
- [ ] 增加景别、角度、姿态、服装、表情和背景覆盖矩阵
- [ ] 为 Anima tags 与 Krea 2 自然语言维护独立 caption 树
- [ ] 冻结数据集版本、文件哈希与 manifest，并导出给 Windows 训练机
- **状态：** pending

### 阶段 19：V1.9 ComfyUI 结果元数据回流
- [ ] 读取 ComfyUI PNG 内嵌 workflow、prompt、seed、sampler、steps、CFG、模型与 LoRA 信息
- [ ] 将结果图关联回创作项目、配方和数据集候选
- [ ] 支持从回流结果创建下一版本，并保留完整来源链
- [ ] 首先支持本地文件/共享目录导入，再考虑直接队列接口
- **状态：** pending

### 阶段 20：V2.0 Windows 交付桥
- [ ] 优先采用 SMB/共享目录交付生成包与训练包
- [ ] 将 4070S 主机定位为 ComfyUI 生成与测试节点
- [ ] 将 5060 Ti 16GB 主机定位为 LoRA 训练与批量测试节点
- [ ] 增加任务状态、结果回传、失败重试和连接诊断
- [ ] 凭据只进入安全存储，不写入项目、日志或生成包
- **状态：** pending / Windows details deferred

### 阶段 21：V2.1 视觉检索与 Krea 2 批量 caption
- [ ] 在 pHash 去重稳定后增加 CLIP embedding 相似图检索与视觉聚类
- [ ] 增加 Git/网站提示词资料的本地缓存、来源、许可和更新时间
- [ ] 建立 FTS + 语义检索的混合召回，检索结果必须保留真实来源
- [ ] 为 Krea 2 增加异步 VLM 自然语言 caption 队列与人工审核
- [ ] 允许将慢速视觉任务按需交给 Windows，Mac 继续负责审核与管理
- **状态：** pending

### 延后阶段：MiniMax H3 视频
- [ ] 绘图 V1 完成后重新确认范围
- [ ] 设计视频项目、时间线、镜头和声音结构
- [ ] 实现 H3 文生视频、图生视频、首尾帧和全参考 Profile
- **状态：** pending / deferred

## 关键问题

1. V1.7 首个真实验收数据集使用哪个目录，是否包含现成 `.txt` caption？
2. V1.8 首个项目类型选 character、outfit、character_outfit 还是 style？
3. Windows 交付首版使用共享目录，还是后续再增加常驻小型代理？

## 已做决策

| 决策 | 理由 |
|------|------|
| 绘图优先，视频延后 | 先形成高频、可完整验收的使用闭环 |
| 保留现有视觉风格 | 用户明确喜欢当前 Prompt Hub 前端风格 |
| 默认首页改为“开始创作” | 当前档案馆首页缺少第一步指引 |
| 使用统一语义项目和模型 Profile | Anima 与 Krea 2 的 Prompt 语法不同，不能共用成品字符串 |
| 首批支持 Anima、Krea 2 | 用户明确要求至少兼容两者 |
| 视频默认 Profile 未来采用 MiniMax H3 | 符合用户实际视频工作流 |
| OC Manager 保持只读交换 | 避免覆盖角色原始数据和扩大同步风险 |
| 现有 API 与数据表作为兼容基线 | 基线测试稳定，新增创作能力无需破坏已有检索与个人资料 |
| 前端先渐进拆分再增加创作入口 | `web.py` 已有 761 行，继续堆叠会放大后续阶段风险 |
| 智能取材先走即时本地检索，模型扩展异步追加 | 避免 Qwen 约 60 秒的延迟阻塞基本取材 |
| 候选必须逐项确认后加入项目 | 防止模型或检索结果未经判断污染槽位 |
| 下一主线先做数据集工作台 | V1.6 已能处理精选结果图，但还不能管理完整训练目录、批量审核与去重 |
| 去重先做 SHA-256 + pHash | 成本低、结果可解释；CLIP embedding 放到视觉检索阶段 |
| ComfyUI 回流先读文件元数据 | 不依赖 Windows 在线，能先闭合结果与配方来源链 |
| Windows 首选共享目录 | 比直接上 SSH/API 更容易部署、排错和恢复，之后再升级传输协议 |
| Mac 负责规划审核，Windows 负责生成训练 | 符合 24GB Air、4070S 主机与 5060 Ti 训练机的设备分工 |

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 进度日志批量补丁目标行位置不匹配 | 2 | 停止批量补丁，读取当前文件并按段落分别更新 |
| V1.6 首次后端补丁重复指定同一文件 | 1 | 确认工具整体拒绝且无部分写入，改为按文件与区段拆分补丁 |
| V1.6 浏览器精确 label 未定位到 Caption Profile | 1 | 页面内嵌 select 使用稳定 `#datasetProfile` 定位继续验收 |
| 阶段 16 首次敏感内容复合正则被 zsh 引号解析拒绝 | 1 | 不读取文件值，改用多条无嵌套引号的独立 `rg -l` 规则 |
| 阶段 16 首次完整验证发现 5 个文件不符合 Ruff format | 1 | 使用项目锁定版本统一格式化，随后从头重跑全部检查 |
| 阶段 16 首次 staged diff 检查发现文档末尾多余空行 | 1 | 修正 `BASELINE.md` 文件末尾并重新暂存、复检 |
| 浏览器按“KREA 2”模糊按钮名定位到 Profile 与导出按钮 | 1 | 改用 `data-profile="krea2"` 精确定位，不重复模糊点击 |
| Gemma 4 12B Heretic 七槽位整理首次调用 120 秒超时 | 1 | 默认优先已加载文本模型，限制 500 tokens、关闭长推理倾向并设 90 秒上限 |
| V1.1 首次批量更新规划文件时补丁段落归属错误 | 1 | 读取精确位置后按文件分别更新，不重复原批量补丁 |
| V1.1 首轮取材测试漏掉 `dress` 分类，lint 报长行与全角逗号 | 1 | 增加精确服装分类亲和词并修正规范问题，不放宽 wildcard 总过滤 |
| 文档搜索使用不存在的 `docs/*.md` glob，zsh 提前终止 | 1 | 改用 `rg --files` 读取仓库中真实 Markdown 路径 |
| 精确动作排序单测的最小资料树没有 `reading` 样本 | 1 | 给测试夹具补充动作与发型样本，验证真实排序而非放松断言 |
| 全量测试发现共享资料夹补样本改变旧 importer 数量断言 | 1 | 撤回共享夹具改动，改为仅在相关性单测中插入专用来源 |
| 运行时检查误用 `/health`，真实端点为 `/api/health` | 1 | 查阅 API 测试确认路径，后续只检查真实端点 |
| 首次新增结果媒体文件时补丁包含不存在的 pyproject 匹配行 | 1 | 确认补丁未部分应用，移除不必要的 lint 配置修改后重新应用 |
| 结果媒体首轮 lint 命中类型导入、异常消息和全角标点规则 | 1 | 移动纯类型导入、修正测试标点，并为媒体校验异常采用与现有领域模块一致的局部忽略 |
| 首次前端大补丁依据截断输出误判重复行，校验失败 | 1 | 读取带行号的精确区段，确认文件只有单行后移除错误匹配条件重新应用 |
| Gemma 4 12B Heretic 真实视觉复盘 180 秒超时 | 1 | 保留为手动选项，日常默认改为用户原规划中的 Qwen3.5 9B 视觉模型并继续实测 |
| Qwen3.5 9B 首次视觉请求立即返回 400，但旧封装丢失错误正文 | 1 | 先增强 HTTPError 正文解析，再依据 LM Studio 的真实原因调整，不重复盲试 |
| Qwen3.5 9B 单模加载后完整视觉 JSON 仍在 180 秒超时 | 1 | 将图片缩到 1024、输出降至 700 tokens，并限制每项长度后重新验证交互规格 |
| 等待 Qwen 视觉请求时一次工具参数引号误写 | 1 | 未影响后台进程，立即改用正确参数轮询并记录真实超时结果 |
| 移动 `/no_think` 时字符串拼接逗号位置错误 | 1 | 在执行前人工复核发现并修正，随后先跑 Python 语法与 lint 检查 |
| 原生视觉 API 输入类型按文档写为 `message` 后返回 400 | 1 | 依据本机服务端校验改为 `text`，随后真实图片调用成功 |
| JavaScript 检查管道未先移除开头空行 | 1 | 先 `strip()` 再移除 script 标签，完整语法检查通过 |
| 浏览器截图误用 Playwright 子对象方法 | 1 | 按当前 Browser API 改用 `tab.screenshot` 并完成桌面/手机取证 |
| 安全策略拒绝永久删除临时 QA 目录 | 1 | 不绕过安全限制，保留在 `/tmp` 交由系统自动清理 |
| V1.3 浏览器分支后等待成功文案超时 | 1 | 停止重复操作，先核对页面、网络与隔离库项目数量 |
| 正式模型状态 jq 假定响应为数组 | 1 | 检查真实 JSON 结构后按对象字段重新查询 |
| 完成前 SQLite 断言误用 `RAISE()` | 1 | 改用 `COUNT(*)` 与 shell 数值断言并重跑全套检查 |
| V1.4 后端整块补丁上下文不匹配 | 1 | 不重复大补丁，读取精确区段后分段应用 |
| V1.4 复盘测试函数 statements 超限 | 1 | 拆出版本建议 API 独立测试，不修改 lint 阈值 |
| 定向 pytest 触发全局覆盖率门槛 | 1 | 保留门槛，最终以全量 pytest 验收 |
| V1.4 浏览器 reload 后丢失当前视图 | 1 | 按默认首页→创作台的真实路径重新进入，不重复直接等待 |
| V1.4 应用建议后提示被异步刷新覆盖 | 1 | 检查实际写入后，将成功提示保存在 state 并由渲染函数读取 |

## 阶段 10 决策

| 决策 | 理由 |
|------|------|
| 下一步做结果图回流与视觉复盘 | 当前流程能导出 Prompt，但生成后只能手填路径，尚未形成“看图—诊断—改进—复用”闭环 |
| 图片与分析默认只在本机处理 | 符合 OC、成人项目和个人结果图的隐私边界 |
| 视觉建议继续采用预览确认流 | 与锁定槽位原则一致，避免模型静默覆盖创作项目 |
| 视觉复盘走 LM Studio 原生 Chat 并关闭 reasoning | Qwen3.5 社区微调会把兼容接口预算耗在隐藏推理，原生开关实测约 63 秒稳定返回 |

## 阶段 11 决策

| 决策 | 理由 |
|------|------|
| 下一步建立项目版本链 | V1.2 已能复盘，但继续修改会覆盖同一项目，无法清楚回答每轮改了什么 |
| iteration 与 revision 分开 | revision 是自动保存次数，iteration 才是 V1/V2/V3 的创作轮次 |
| 新版本不复制旧结果图 | 每一轮结果应归属自己的项目，避免图库混淆和重复占用 |
| 首版不自动覆盖槽位 | 模型建议作为新版本说明保留，具体修改继续由用户确认 |

## 阶段 12 决策

| 决策 | 理由 |
|------|------|
| 下一步补版本对照和建议应用 | V1.3 已建立链路，但用户仍需手工判断父版本内容与复盘建议的关系 |
| 建议只填空且未锁定槽位 | 保留用户判断，防止视觉模型覆盖角色和已确定创作内容 |
| 不新增数据库列 | `lineage_json` 已能承载复盘观察，避免连续迁移正式资料库 |
| 父项目缺失时降级显示 lineage 摘要 | 导入旧 JSON、手工清理或历史数据不完整时页面仍应可用 |

## 阶段 13 决策

| 决策 | 理由 |
|------|------|
| 精选状态保存在 `generation.result_assets` | 兼容现有项目 JSON，不为附加元数据引入数据库迁移 |
| 只打包用户明确精选的本地结果图 | 外部链接不可保证可用性与隐私，且不能替用户判断训练素材 |
| Anima 与 Krea 2 使用独立 caption Profile | 前者偏 tags，后者偏自然语言，不能混为一种训练文本 |
| ZIP 只写入本机 `exports/datasets` | 不改动原图、不自动上传 Windows，先完成可审计的本地闭环 |

## 备注

- 详细产品和技术路线见 `DEVELOPMENT_PLAN.md`。
- V1.3 回归边界见 `BASELINE.md`。
- 不自动 commit、push 或创建 PR。
- 每完成一个阶段，更新本文件与 `progress.md`。
- 绘图 V1.5 已部署到 `127.0.0.1:8765`；两台 Windows 的自动传输继续延后。
