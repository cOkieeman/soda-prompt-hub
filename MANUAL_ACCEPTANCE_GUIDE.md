# Prompt Hub · 情景工作流与人工验收

这份说明只讲“遇到什么情况，要去哪里、按什么顺序做”。日常操作和人工验收都不要求打开终端。

## 先记住三个边界

1. Mac 是事实中枢：保存项目、提示词、资料来源、结果图、审核状态、Caption、数据集版本和任务历史。
2. Windows 5060 Ti 是执行设备：负责 ComfyUI 出图，以及你在 AnimaLoraStudio 中自行完成的筛标、正则和训练。
3. 自动结果都是草稿：模型建议、WD14、Krea 2 视觉描述和相似图都不能代替人工审核。

## 情景一：从灵感到一张图

1. 双击 `启动-Prompt-Hub.command`，打开“创作台”。
2. 新建项目，写中文想法；有 OC 时也可以从“角色库”以角色开始。
3. 用“从本地库智能取材”补充七个槽位，需要时加入视觉参照。
4. 分别检查 Anima 与 Krea 2 Prompt；两种格式各自保存，不互相覆盖。
5. Windows 在线时，在“模型、LoRA 与采样参数”中选择底模、LoRA、尺寸、Steps、CFG 等，先勾选“低成本测试”。
6. 发送到 5060 Ti，在“设备连接 → 任务状态”等待“Windows 已回传”。
7. 点“验收并导入图片”。满意则加入数据集，不满意则记录问题或“由此创建下一版”。

## 情景二：Windows 关机，但我想继续创作

1. 正常打开 Prompt Hub，继续写项目、查资料、整理 Prompt、建立配方和准备数据集。
2. 不点击“发送到 5060 Ti”或“复制到 5060 Ti”。
3. 需要出图时先保存项目；Windows 恢复后再发送。

Windows 离线不会影响 Mac 已保存的项目、资料、Caption、结果图和数据集版本。

## 情景三：从出图结果到打好标的数据集

1. 在创作台结果图中只勾选真正想用于数据集的图片。
2. 点项目总览中的“送入数据集工作区”。新进入的图片仍是“未审核、未选择”。
3. 按数据集五步走：导入素材 → 检查问题 → 准备标签 → 人工审核 → 冻结交付。
4. Anima 优先使用已有 `.txt`，没有时用 WD14 生成草稿；Krea 2 用英文自然语言 Caption。
5. 打开图片逐张确认，决定保留、待复查或排除，并确认目标 Profile 的 Caption。
6. 运行正式交付前检查。处理完红色阻塞项后，冻结到 Mac。
7. Windows 在线且共享盘已挂载时，再从交付历史点“复制到 5060 Ti”。

冻结只建立副本和 ZIP，不改原图、原 `.txt`，也不启动训练。

## 情景四：整理以前做好的外部数据集

1. 在“数据集”输入旧数据集文件夹路径并读取。
2. 它会显示为“外部独立数据集”，不需要绑定创作项目。
3. 先检查坏图、缺 Caption、完全重复和近似重复。
4. 有旧 `.txt` 时先预览接续范围，再接入 Anima 或 Krea 2；不要直接全部重跑 WD14。
5. 人工确认图片与 Caption 后再冻结。

Prompt Hub 只保存工作区状态和派生版本。外部原始数据集文件夹不在 Prompt Hub 备份内，需要另行备份。

## 情景五：更新提示词库或保存网页资料

公共 Git 资料更新：打开“资料管理”，点“更新公共提示词库”。有本地改动的来源会跳过，不会强行覆盖。

保存网页资料：粘贴原始 URL，填写标题、用途、分级和许可。GitHub/raw 白名单内容可以受限离线保存；Civitai 等站点通常只保存链接和备注，方便以后回看模型说明与提示词。

新增或改动很多视觉图片后，再到“智能检索”点“建立 / 更新本地索引”。增量更新会跳过未变化的图片。

## 情景六：项目或精选图发生变化

项目总览显示“需要更新”时，点“更新并打开工作区”。系统按来源 ID 和 SHA-256 同步，不会重复复制同一张图。

同步后仍需重新看数据集状态。新增图片不会自动继承“保留”或“Caption 已确认”。已有冻结版本保持不变；新的修改要生成下一个版本。

## 情景七：SMB 共享盘突然断开

1. 停止点击复制或投递，不要手动覆盖共享目录里的半成品。
2. Mac 上已经冻结的版本和 ZIP 会继续保留。
3. Windows 开机后，在 Finder 重新连接服务器并挂载共享盘。
4. 打开“设备连接”，点“检查挂载”。看到“共享目录已就绪”或“Windows 已就绪”后再重试。
5. 同一版本已经完整存在时会显示“5060 Ti 已有此版本”；同名但哈希不同会停止，不会覆盖。

## 情景八：Prompt Hub 页面打不开或意外退出

1. 双击 `诊断-Prompt-Hub.command`。
2. 如果只是服务未运行，再双击 `启动-Prompt-Hub.command`。
3. 重新打开页面后，检查最近项目和数据集工作区是否仍在。
4. 被中断的本地扫描、WD14 或 Krea 2 草稿任务会回到等待队列并继续；如果页面显示失败，再使用对应任务的“重试”。

不要在任务正在写入时连续强制关闭。必须关机时，先在页面取消正在运行的任务。

## 情景九：冻结失败或磁盘空间不足

1. 先读页面的中文失败原因，不要反复点击。
2. 磁盘不足时先清理 Mac 空间，再重新冻结。
3. 来源图片在扫描后变化时，先重新扫描并重新审核。
4. 冻结失败会清理未完成目录和 ZIP；旧版本、源图片和原 `.txt` 保持不变。

## 情景十：备份与隔离恢复

备份：双击 `备份-Prompt-Hub.command`。它会同时建立备份和校验，默认保存到 `soda-person/backups/prompt-hub`。

备份包含：主数据库、视觉索引、项目、个人资料、结果图、数据集工作区、项目精选来源、冻结交付版本、LoRA 项目、Workflow Profile 和设备登记。

备份不包含：外部原始数据集、可重新同步的公共 Git 库和 Windows 预览缓存、大模型权重、Windows 上的 LoRA/底模权重。

恢复只允许进入一个新的空目录，不会覆盖正式资料库。需要恢复时可以把备份目录交给 Codex，由它先做校验和隔离恢复；核对无误后再单独决定是否切换。

## 人工验收清单

建议按下面顺序检查，全程只看页面，不改“林悔儿”的审核状态：

- [ ] 双击启动脚本后，首页可以打开，刷新页面不会丢失当前项目。
- [ ] 创作台顶部能看到六张项目状态卡，按钮能带到对应区域。
- [ ] Anima 与 Krea 2 Prompt 可以切换，内容不会串到一起。
- [ ] “智能检索”能进行关键词检索、图片查相似图，并显示真实来源。
- [ ] “资料管理”能看到本地 Git 与网页来源、许可和更新时间。
- [ ] “数据集”左侧工作区显示“可使用 · 72 张”，不再显示原始 `ready`。
- [ ] 打开“林悔儿”后，仍是 72 张未审核、Anima 72/72 已确认、Krea 2 0/72、没有正式导出。
- [ ] 数据集简单模式能按五步切换；未人工审核时冻结按钮保持禁用。
- [ ] “设备连接”只显示中文状态；Windows 离线时能明确提示共享目录未挂载，并且 Mac 页面仍可正常使用。
- [ ] LoRA 与底模是独立子页面，目录树、搜索、预览图和 Civitai 来源仍能使用。
- [ ] 双击诊断脚本能看到数据库、视觉索引、WD14、磁盘和服务的检查结果。
- [ ] 双击备份脚本能生成一个新备份；交给 Codex做隔离恢复后，数据库和关键目录校验一致。

验收时只要某一项不符合，记下页面名称、点了什么、看到什么提示；不要为了继续而批量确认或覆盖数据。
